import { NextRequest,NextResponse } from "next/server";import{generateAuthenticationOptions}from"@simplewebauthn/server";import{ensureAuthIndexes,normalizeUsername}from"@/lib/auth";
export async function POST(request:NextRequest){const body=await request.json().catch(()=>null);const db=await ensureAuthIndexes();const user=await db.collection("users").findOne({usernameNormalized:normalizeUsername(String(body?.username||""))});if(!user?.passkeys?.length)return NextResponse.json({error:"No passkey is registered for that account"},{status:404});const options=await generateAuthenticationOptions({rpID:request.nextUrl.hostname,userVerification:"required",allowCredentials:user.passkeys.map((p:{id:string})=>({id:p.id}))});await db.collection("auth_challenges").updateOne({userId:user._id,type:"login"},{$set:{challenge:options.challenge,expiresAt:new Date(Date.now()+300000)}},{upsert:true});return NextResponse.json(options);}
